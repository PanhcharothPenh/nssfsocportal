{
  "name": "nssfsocportal-root",
  "version": "1.0.0",
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "build": "node sync-backend.js && npm --prefix frontend install --include=dev && npm --prefix frontend run build"
  }
}
